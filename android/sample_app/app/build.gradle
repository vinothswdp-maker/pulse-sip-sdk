plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
}

repositories {
    google()
    mavenCentral()
    maven { url "https://vinothswdp-maker.github.io/pulse-sip-sdk/repo" }   // the real, live, hosted URL
    maven { url "https://storage.googleapis.com/download.flutter.io" }
    maven { url "https://jitpack.io" }
}

android {
    namespace = "com.pulse.sampleapp"
    compileSdk = 36

    defaultConfig {
        applicationId = "com.pulse.sampleapp"
        minSdk = 24
        targetSdk = 36
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
        profile {
            initWith debug
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = "11"
    }
}

dependencies {
    
       implementation "com.pulse:pulse_sip_android_sdk:1.0"

    implementation "androidx.core:core-ktx:1.13.1"
}
